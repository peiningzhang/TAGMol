model:
  checkpoint: ./pretrained_models/pretrained_diffusion.pt
  # VEDA sampling (optional overrides)
  time_scheduler: log_uniform  # ['log_uniform', 'arcsin', 'edm', 'edm1']

sample:
  seed: 2021
  num_samples: 10
  num_steps: 1000
  pos_only: False
  center_pos_mode: protein
  sample_num_atoms: prior
